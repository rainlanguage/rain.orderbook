query VaultsQuery {
  tokenVaults(orderBy: owner__id, orderDirection: desc) {
    id
    owner {
      id
    }
    vaultId
    token {
      id
      name
      symbol
      decimals
    }
    balanceDisplay
    balance
  }
}
