query VaultsQuery {
  vaults(orderBy: owner__id, orderDirection: desc) {
    id
    owner {
      id
    }
    tokenVaults {
      id
      token {
        name
        symbol
        decimals
        totalSupply
        totalSupplyDisplay
      }  
    	balanceDisplay
   	 	balance
    }
  }
}