query IO($id: String) {
  io(id: $id) {
    id
    token {
      id
    }
    decimals
    vaultId
    vault {
      id
    }
    order {
      id
    }
    tokenVault {
      id
    }
    index
  }
}
